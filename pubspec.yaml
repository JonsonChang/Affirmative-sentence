name: affirmative_sentence
description: A mobile app for daily positive affirmations

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^14.0.0
  sqflite: ^2.2.0
  path_provider: ^2.0.15

flutter:
  uses-material-design: true
  assets:
    - assets/
